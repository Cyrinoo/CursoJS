
    // Arrayss
        var nomes = [
            'guilherme', 'Pedro', 'Apolo'
        ];
        // para alterar a ordem
        nomes[0] = 'pedro';

        // adicionar itens depois do array ser declarado 
        // adiciona pro final da fila no caso o [3]
        nomes.push('João'); 

        console.log(nomes[0]);

